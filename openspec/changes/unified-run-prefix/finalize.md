# Finalize Receipt

> Generated by the finalize artifact's git-side closeout execution (or hand-authored
> after the escape-hatch flow), marking git-side closeout before /opsx:archive.
> Overwritten if you re-enter finalize (e.g., re-running the git-side closeout or moving
> from "keep as-is" to PR later).

**Change**: `unified-run-prefix`
**Finalized at**: `2026-09-04 22:44`
**Outcome**: `pr-updated`

Hand-authored via the escape hatch. The canonical git-side closeout did not apply: apply ran in
the main checkout rather than a `.worktrees/unified-run-prefix/` worktree, so steps 1-7 (worktree
discovery, `--ff-only` merge, worktree cleanup, branch deletion) had no subject. The merge itself
had already happened by hand before finalize was entered.

---

## Branch state

- **Branch**: `feat/baas-cli-openspec-test`
- **Base branch**: `main`
- **Final state**: `merged`
- **PR URL**: `https://github.com/wsztajerowski/benchmark-as-a-service/pull/53`

Merged manually at `2026-09-04T20:37:17Z` as `63eb948`, with `mergeStateStatus: UNSTABLE` — GitHub
was not blocking the merge (`mergeable: MERGEABLE`, no review required); the `e2e-cloud-test` checks
were red. See *Known-red checks* below.

---

## Workspace

- **Worktree**: `N/A (normal repo)`
- **Cleanup**: `N/A (normal repo)`

---

## PR comment

- **Comment status**: `skipped (deferred — PR already merged)`

The code-reviewer onboarding comment was not posted. The subroutine exists to orient a code
reviewer before review; PR #53 was already merged when finalize ran, so the comment would land
after the moment it serves. Deferred to the user rather than posted unilaterally.

---

## Tests

- **Baseline status at finish**: `passing`

Full reactor `mvn -B clean verify` with `ASYNC_PATH` exported, at `9e50b58`: BUILD SUCCESS, 389
tests, no failures, errors or skips — `baas-model` 51; `benchmark-runner` 36 unit + 15 IT;
`baas-cli` 271 unit + 16 IT. Both store contract suites pass against their own adapters, and the
async-profiler IT ran rather than silently skipping.

---

## Next step

**Not the standard `pr-updated` sequence.** That bullet reads "wait for code review, then archive,
push, merge" — the merge has already happened, so only the archive half remains, and it is blocked.

**Do not archive yet.** 14 tasks are open in `tasks.md`, and they include the live deploy and the
one-way migration. Archiving now would sync delta specs describing behaviour that has not been
exercised against a real account, and section 11's migration is the change's only irreversible step.

Gating order, unchanged from `verify.md` §Next step:

1. **2.5 — release.** In flight at finalize time: `release.yml` run `33917138378` on `63eb948`,
   triggered by the merge. Nothing below is exercisable until it publishes.
2. **12.2 / 12.2a — infrastructure.** `baas admin setup`, then the CI stack redeploy.
3. **12.3-12.8 — manual end-to-end**, from a released CLI. 1.6's inventory must be taken *before*
   12.3, not merely before 11.4.
4. **11.4-11.7 — migration.** Dry-run read against 1.6's counts, real pass, spot-check, delete the
   script.
5. Then `/opsx:archive`.

Blocked on credentials at finalize time: AWS (`aws sts get-caller-identity` reports no credentials).
GitHub auth was restored during this session, which is what allowed 1.1 and 1.2 to be closed.

---

## Known-red checks at merge

Recorded because the merge went in over them, deliberately. Neither is fixed by anything on the
branch, and `ci-pr-build` — the ordinary Maven build — passed.

| Cause | Status |
|---|---|
| `WorkflowRole` denied `s3:PutObject` on `runs/*` | The template grant from 8.2 was never deployed. Now tracked as **12.2a**, which did not exist before finalize. |
| `GHA_EC2_PAT` expired — "GitHub Registration Token receiving error / Bad credentials" | Repo secret needs regenerating. Outside this change. |
| SSM mongo gate in `exec-single-benchmark.yml` | Did not run (its jobs were skipped upstream). Still broken by the DynamoDB cutover — unclaimed work per CLAUDE.md, outside this change. |

## Post-merge state

`org.jsoup:jsoup` Dependabot alert #2 (`GHSA-pmhh-3w7g-xqp8` / `CVE-2026-71497`, medium, patched in
1.23.1) moved to `state: fixed` at `2026-09-04T20:37:21Z` — closed by the merge, since `b9a5b99`
carried jsoup to 1.23.2. The repository now has zero open Dependabot alerts.
