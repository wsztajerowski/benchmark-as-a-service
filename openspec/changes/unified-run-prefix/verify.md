# Verification Report

> This file is generated by the `openspec-verify-change` skill after apply completes,
> to confirm that the implementation is consistent with specs / design / tasks.
> Failed checks must be fixed in the corresponding artifact, then re-run verify.

**Change**: `unified-run-prefix`
**Verified at**: `2026-08-21 10:12`, warnings resolved `2026-08-21 10:41`
**Iteration**: `1`
**Verifier**: `Claude Opus 5 (same session as apply)`

---

## 1. Structural Validation (`openspec validate --all --json`)

- [x] All items `"valid": true` — **for this change**

**Result**:

```text
total: 9   invalid: 2
✗ export-before-teardown            — "Change must have at least one delta. No deltas found."
✗ gha-workflow-migration-to-dynamodb — "Change must have at least one delta. No deltas found."
```

`openspec validate unified-run-prefix` → **valid**.

| Item | Type | Issues |
|---|---|---|
| `export-before-teardown` | other change | No deltas — brainstorm-only scaffold. **Not this change's concern.** |
| `gha-workflow-migration-to-dynamodb` | other change | No deltas — scaffold only. **Not this change's concern.** |

Both pre-date this change and were untouched by it (`git status` shows `export-before-teardown/brainstorm.md`
still untracked from before this session).

---

## 2. Task Completion (`tasks.md`)

- [ ] All `- [ ]` have been changed to `- [x]` — **50 of 65**

**Incomplete tasks**:

| Task | Reason incomplete | Blocks archive? |
|---|---|---|
| 1.1, 1.2 | Need a GitHub token — `gh auth status` reports the keyring token invalid. Confirm the published runner JAR carries the placeholder version and that no `baas-cli.jar` asset exists yet. | No — informational. If 1.1 is false, Task 1 was over-built, but no code changes. |
| 1.6 | Needs AWS credentials — inventory the bucket and table. 11.4 checks the dry run against these counts. | **Yes** — 11.4 cannot be read meaningfully without it. |
| 2.5 | **Cut a real release.** Gates 12.2–12.8: until a release carries its own version *and* a `.sha256`, `RunnerJarResolver` has nothing to pin to. | **Yes** |
| 11.4–11.7 | Dry-run, run, spot-check and then delete `scripts/migrate-run-layout.sh`. Needs AWS credentials. | **Yes** — 11.7 leaves a script in the tree that the change says to delete. |
| 12.2–12.8 | Manual end-to-end verification against a real account. | **Yes** — no automated test drives `baas run`, so these are the only proof the cutover works. |

**None of these is implementation work.** Every code, infrastructure, CI and documentation task is
complete and covered by tests; the remainder is a credentials gate.

---

## 3. Delta Spec Sync State

| Capability | Sync status | Notes |
|---|---|---|
| `benchmark-results-query` | ✗ pending sync | MODIFIED — exists in `openspec/specs/` |
| `cli-command-structure` | ✗ pending sync | MODIFIED — exists in `openspec/specs/` |
| `core-stack-provisioning` | ✗ pending sync | ADDED + MODIFIED — exists in `openspec/specs/` |
| `results-store-schema` | ✗ pending sync | MODIFIED — exists in `openspec/specs/` |
| `runner-image-provisioning` | ✗ pending sync | MODIFIED — exists in `openspec/specs/` |
| `run-artifact-layout` | ✗ pending sync | **New capability** — no counterpart yet; created at archive |
| `run-identity` | ✗ pending sync | **New capability** — no counterpart yet; created at archive |
| `runner-jar-distribution` | ✗ pending sync | **New capability** — no counterpart yet; created at archive |

Pending sync is the expected state before archive. **No `## REMOVED Requirements` block exists in
any delta**, so the archive hazard CLAUDE.md records — a REMOVED heading that paraphrases the main
spec's requirement name, which `openspec archive` warns about and then archives anyway — does not
apply here. Nothing to grep for before archiving.

---

## 4. Design / Specs Coherence Spot Check

| Sample item | design description | specs counterpart | Gap |
|---|---|---|---|
| Run id shape | `<UTC instant, ISO basic, ms>Z-<8 hex>`, 28 chars, `SecureRandom` | `run-identity` → "The run identifier is time-ordered and collision-resistant" | None — `RunId.java:24,27,29`; pinned by `RunIdTest` (7 tests) |
| One clock read | CLI mints the instant, passes it as `--created-at` | `run-identity` → "The launching CLI reads the clock once per run" | None in code (`RunCommand.java:213-215`, `ApiCommonSharedOptions.java:76-83`); **test gap — see W3** |
| `runs/<project>/<runId>/` | Prefix is the run; `input/` sub-prefix | `run-artifact-layout` → "One run occupies one S3 prefix" | None — `RunLayout.java`; no run-prefix concatenation exists outside it (verified by grep) |
| Runner JAR pinned + verified | Upload-if-absent, `.sha256` checked before upload | `runner-jar-distribution` → 4 requirements | None in code; **reporting gap — see W2** |
| Versioning suspended, `runs/` expiry deleted | Both load-bearing, shipped together | `core-stack-provisioning` | None — `cf-template-core.yaml`; pinned by 3 new `CoreTemplateTest` assertions |
| `branch` becomes a tag | Caller-supplied, not machine-observed | `results-store-schema` → tag vocabulary | None — `TagKeys.BRANCH` in `KNOWN`, absent from `MACHINE_OBSERVED`, pinned by `TagKeysTest` |
| Migration: keys never touched | Copy, then `UpdateItem` on four path attributes | `run-artifact-layout` → "Existing runs are relocated without altering their stored keys" | None — script self-tested; emits no `pk`/`sk` write |
| `RESULT#unknown` → `runs/unknown/` | Relocated, never re-attributed | `run-artifact-layout` scenario | None — covered by the script's synthetic-scan test |

**Drift warnings** (non-blocking):

- design.md's *Migration Plan* step 1 says "`release.yml` first … Nothing downstream works without
  a release that carries its own version." The code shipped in the opposite order — everything is
  written, and the release is the outstanding gate. This is a sequencing note, not drift: the
  reactor hard-fail is exactly what makes the missing release loud rather than silent, as design.md
  itself anticipates under *Risks / Trade-offs*.

---

## 5. Implementation Signal

- [x] No unstaged files in the worktree relating to this change
- [x] All related commits have been pushed — **pushed**; `feat/baas-cli-openspec-test` and
      `origin/feat/baas-cli-openspec-test` are both at `722e5d2` (`git ls-remote` confirms the
      remote ref). The earlier "13 commits sit on the local branch" reading was taken before the
      final push and is superseded.

**Commit range**: `f9b46d8..HEAD` on `feat/baas-cli-openspec-test` (base `82d36f8`)

`openspec/changes/export-before-teardown/brainstorm.md` is untracked but belongs to a different
change and pre-dates this session.

---

## Findings

### CRITICAL

1. **15 tasks incomplete** (§2). All credential-gated; none is implementation work. This change
   must not be archived until 2.5 → 12.2–12.7 → 11.4–11.7 have run, because no automated test
   drives `baas run` end to end and the migration is the change's only one-way door.

### WARNING — all three fixed in `94d5af3`

1. **`DownloadCommand` has no results-table guard.** `baas-cli/.../commands/DownloadCommand.java:77-79`
   passes `config.getAws().getResultsTable()` straight into `ResultsQueryService` on the run-id
   branch. The bucket has an explicit guard eight lines above (`:67-70`); the table does not, so an
   unsynced config turns `baas download <runId>` into a raw SDK validation error instead of the
   actionable "run `baas config sync`" message.
   **Fixed** in `94d5af3`: `tableUnresolvable(...)` guards the branch and reports the
   `baas config sync` command, matching the bucket check. Split out as a package-private method so
   the guard is reachable in a unit test without AWS credentials.

2. **`baas run` never reports the pinned runner version at default verbosity.**
   `runner-jar-distribution` requires "The CLI reports the version it pinned" (scenario: *the CLI
   reports which runner version the run will execute*). `RunnerJarResolver.java:52` logs the reuse
   path — the common case, every run after the first for a version — at `debug`. Only the seeding
   path logs at `info` (`:55`).
   **Fixed** in `94d5af3`: both branches now report at `info` — the pinned key with the CLI
   version it came from, and a `--runner-jar` override as an explicit local override.

3. **No test pins that the stored `createdAt` is the caller-supplied instant.** The only assertion
   is at the options boundary (`ApiCommonSharedOptionsTest:95`). Nothing covers
   `JmhRunResults.java:51` or `JCStressSubcommandService.java:88` actually using
   `commonOptions.createdAt()` — reverting either to `Instant.now()` would compile and pass every
   test, silently defeating the change's central property.
   **Fixed** in `94d5af3`: `JmhStoreIntegrationIT.theStoredTimestampIsTheCallersInstantNotOneReadHere`
   runs the service with a fixed past instant and asserts the stored `createdAt` equals it.
   **Verified by reverting** `JmhRunResults.java:51` to `Instant.now()`: the new test failed
   (`expected: 2026-08-20T17:44:32.812Z but was: 2026-08-21T09:32:22.494Z`) while the 13-test unit
   suite still passed — exactly the gap this warning named. Covers `JmhRunResults`, shared by the
   three JMH-flavoured services.

   `JCStressSubcommandService.java:88` — the fourth service, and the only one `JmhRunResults` does
   not serve — is pinned in `b9122a6`, inside the existing `JCStressSubcommandServiceIT` scenario
   rather than a second test, because a JCStress run costs ~58s. Asserted through the stored
   document id: `MongoResultsStore` builds it as `partitionKey|sortKey` and `ResultKeys.sortKey`
   formats `measurement.createdAt()` directly, so it is a plain String with no Morphia type mapping
   in the way, and it additionally proves the value reached the key the store orders and
   de-duplicates on. Also verified by reverting the line. **All four services are now pinned.**

### SUGGESTION

1. **`runner.sourceRepo` is undocumented for users.** `runner-jar-distribution` asks for "a
   configuration value with a **documented** default". It exists and round-trips
   (`BaasConfig.java:109`), but appears in no user-facing doc and has no `baas config set` option —
   only a javadoc and a line in `docs/review/baas-cli-findings.md`.
   **Fix**: one row in CLAUDE.md or `infra/README.md`'s config section.

2. **`RunCommand` appends the two input filenames by hand** (`:238`, `:247`:
   `inputPrefix + "/benchmark.jar"`, `inputPrefix + "/runner.jar"`). The *prefix* comes from
   `RunLayout`, so the invariant holds, but `RunLayout.benchmarkJarKey(project, runId)` and
   `runnerJarOverrideKey(...)` would put the two object keys in the same place as everything else.

---

## Overall Decision

- [ ] ✅ PASS
- [x] ⚠️ **PASS WITH WARNINGS** — code, infrastructure, CI and docs are complete, internally
      consistent and test-covered; the three warnings are small and none blocks the remaining
      manual gate. **But the change is not archivable**: 15 tasks remain, all requiring credentials
      this session lacks, and they include the live deploy and the one-way migration.
- [ ] ❌ FAIL

Recording this as PASS WITH WARNINGS rather than FAIL is a judgement about *kind*: nothing found is
a defect in what was built. The incomplete tasks are a human gate the change always had, not work
that was skipped.

**Next step**:

1. ~~Apply the three warnings.~~ **Done** — `94d5af3`, plus `b9122a6` pinning the fourth service.
   Full reactor `mvn -B clean verify` with a real `ASYNC_PATH` is green afterwards: 269 unit + 16
   CLI ITs, `JmhStoreIntegrationIT` 3/3, `JCStressSubcommandServiceIT` 1/1, and
   `JmhWithAsyncProfilerSubcommandServiceIT` 1/1 with `Skipped: 0`.
2. **Cut a release** (tasks.md 2.5). Nothing below is exercisable without one.
3. `baas admin setup`, then the manual run checks (12.2–12.7). Note the gate order now
   recorded in `tasks.md` 2.5: `release.yml` fires only on a push to `main`, so step 2's
   release requires merging this branch first — steps 3–5 run post-merge, not on the
   feature branch.
4. Migration last: dry-run, read it against 1.6's counts, then the real pass and delete the script
   (11.4–11.7).
5. Then `/opsx:continue` → finalize → `/opsx:archive`.

> **Convergence loop reminder**:
> - PASS / PASS_WITH_WARNINGS → `/opsx:continue` advances to the finalize
>   artifact, which executes the git-side closeout directly (merges
>   worktree → feature branch, pushes to update existing PR, posts
>   code-reviewer onboarding comment). The skill
>   `superpowers:finishing-a-development-branch` is an escape hatch for
>   non-canonical flows. Then `/opsx:archive` once finalize.md exists.
> - FAIL with items fixable by code change → return to the apply artifact
>   and re-run `/opsx:apply`, then re-run `/opsx:verify` — this overwrites
>   both apply.md and verify.md with the next iteration.
> - FAIL with artifact-level items → fix the offending artifact, then
>   re-enter apply.
> - Iteration > 5 → stop the loop and report to the user.
