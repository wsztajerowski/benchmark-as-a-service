# Finalize Receipt

> Generated by the finalize artifact's git-side closeout execution (or hand-authored
> after the escape-hatch flow), marking git-side closeout before /opsx:archive.
> Overwritten if you re-enter finalize (e.g., re-running the git-side closeout or moving
> from "keep as-is" to PR later).

**Change**: `installable-cli-command`
**Finalized at**: `2026-09-17 16:49`
**Outcome**: `merge-locally`

Hand-authored after the escape-hatch flow. The canonical git-side closeout does not apply here,
and its own instruction names this case: the session sits on the integration branch (`main`),
there is no `.worktrees/installable-cli-command/` and no branch of that name, and the
implementation was merged and released before finalize ran — nothing remained to merge, push or
open. `superpowers:finishing-a-development-branch` was invoked as the escape hatch directs; all
three of its options presume unintegrated work on a feature branch, so none applied.

---

## Branch state

- **Branch**: `main`
- **Base branch**: `main`
- **Final state**: `merged`
- **PR URL**: `N/A` — no PR targets `main`. The change reached `main` through
  [#57](https://github.com/wsztajerowski/benchmark-as-a-service/pull/57) (implementation, merged
  2026-09-12), with follow-ups
  [#58](https://github.com/wsztajerowski/benchmark-as-a-service/pull/58),
  [#59](https://github.com/wsztajerowski/benchmark-as-a-service/pull/59) and
  [#60](https://github.com/wsztajerowski/benchmark-as-a-service/pull/60).

`HEAD` and `origin/main` are both `451a33a`.

---

## Workspace

- **Worktree**: `N/A (normal repo)`
- **Cleanup**: `N/A (normal repo)`

`GIT_DIR` and `GIT_COMMON` both resolve to `<repo>/.git`, so the skill's Step 2 detection reports a
normal repo with no worktree to clean up. No `.worktrees/` directory exists.

---

## PR comment

- **Comment status**: `skipped (no PR)`

The code-reviewer onboarding subroutine self-skips: `gh pr view main` reports no pull request for
the branch. The PRs that carried this change are all merged.

---

## Tests

- **Baseline status at finish**: `passing`

`mvn -B clean verify` at `451a33a`: BUILD SUCCESS, 6/6 modules, **401 tests** across surefire and
failsafe (51 + 36 + 15 + 283 + 16), zero failures, errors or skips, 7:05 min. Run on this tree
rather than carried over from an earlier session — a green run only proves the tree it ran on.
`JmhWithAsyncProfilerSubcommandServiceIT` did execute here because `ASYNC_PATH` is exported
locally; it silently skips in CI, which is open finding D3 and unchanged by this change.

---

## Next step

Run `/opsx:archive` on main to sync delta specs and move the change directory into the archive.

**Archive remains blocked, and finalize does not unblock it.** `tasks.md` stands at 63/65:

- **8.5** — the clean-machine zero-to-first-result walk. Its remaining steps, `admin setup` and
  `admin build-image`, would rebuild live infrastructure, so it needs a separate account or a
  genuinely clean machine.
- **9.2** — the Linux leg of installing from the published one-liner, deferred by decision to the
  CI-changes work. The macOS leg is done: v2.2.0 by fresh install, and v3.0.0 via `--update`.

Six of the eight manual tasks that verify.md recorded as blocking archive have closed since it was
written — 9.1, 9.3, 9.4, 9.6 and 9.7 on 2026-09-12, and 9.5 on 2026-09-15, once v3.0.0 supplied an
older *installable* release to upgrade from.

Verify outcome carried forward: **PASS_WITH_WARNINGS** (verify.md, iteration 2); apply iterations:
2. W3 is closed, and W4h closed when the published v3.0.0 installer proved it installs its own
release. W1, W2 and W4 a/b/g remain.
